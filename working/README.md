# Working

The `working/` directory contains source material, drafts, research, photographs, and project notes that are used to create published content.

Unlike `content/`, material in `working/` is not intended for direct publication. Instead, it serves as the authoritative working copy from which published documents, webpages, and other artifacts are derived.

Nothing in this directory should be considered temporary unless explicitly marked as such.
