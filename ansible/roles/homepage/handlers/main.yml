---
- name: Restart homepage container
  ansible.builtin.command:
    cmd: docker restart homepage
  changed_when: true

- name: Redeploy Homepage
  ansible.builtin.command:
    cmd: docker compose up -d
    chdir: "{{ homepage_compose_dir }}"
