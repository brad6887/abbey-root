# Abbey Root Automation

Generated automatically by Ansible.

## Playbooks

| Playbook | Description |
|----------|-------------|
{% for playbook in query('fileglob', playbook_dir + '/*.yml') | sort %}
{% set playbook_name = playbook | basename %}
{% set playbook_base = playbook_name | replace('.yml', '') %}
{% set playbook_doc = playbook_dir + '/docs/' + playbook_base + '.md' %}
{% set playbook_doc_exists = lookup('fileglob', playbook_doc, wantlist=True) | length > 0 %}
| {{ playbook_name }} | {% if playbook_doc_exists %}{{ lookup('file', playbook_doc).split('\n')[2] | default('') }}{% else %}No documentation found{% endif %} |
{% endfor %}

## Roles

| Role | Description |
|------|-------------|
{% for role in ansible_roles.files | sort(attribute='path') %}
{% set readme_path = role.path + '/README.md' %}
{% set readme_exists = lookup('fileglob', readme_path, wantlist=True) | length > 0 %}
| {{ role.path | basename }} | {% if readme_exists %}{{ lookup('file', readme_path).split('\n')[2] | default('') }}{% else %}No README found{% endif %} |
{% endfor %}
