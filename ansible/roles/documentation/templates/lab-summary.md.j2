# Abbey Root Lab Summary

> **Generated automatically by Ansible.**
>
> Do not edit this file manually.

---

## Overview

| Item | Count |
|------|------:|
| Hosts | {{ groups['all'] | length }} |
{% set docker_host_count = namespace(value=0) %}
{% for host in groups['all'] %}
{% if hostvars[host].docker_host | default(false) %}
{% set docker_host_count.value = docker_host_count.value + 1 %}
{% endif %}
{% endfor %}
| Docker Hosts | {{ docker_host_count.value }} |
{% set homepage_host_count = namespace(value=0) %}
{% for host in groups['all'] %}
{% if hostvars[host].homepage_show_server | default(false) %}
{% set homepage_host_count.value = homepage_host_count.value + 1 %}
{% endif %}
{% endfor %}
| Homepage Hosts | {{ homepage_host_count.value }} |

{% set service_count = namespace(value=0) %}
{% for host in groups['all'] %}
{% if hostvars[host].homepage_services is defined %}
{% for service_group in hostvars[host].homepage_services %}
{% set service_count.value = service_count.value + (service_group.services | length) %}
{% endfor %}
{% endif %}
{% endfor %}

| Homepage Services | {{ service_count.value }} |

---

## Hosts

| Host | IP Address | Description |
|------|------------|-------------|
{% for host in groups['all'] | sort %}
| {{ host }} | {{ hostvars[host].primary_ip | default(hostvars[host].ansible_host | default('Unknown')) }} | {{ hostvars[host].host_description | default('Unknown') }} |
{% endfor %}

---

## Documentation Outputs

Generated documentation currently includes:

- `architecture.md`
- `automation.md`
- `containers.md`
- `inventory.md`
- `network.md`
- `servers.md`
- `services.md`
- `lab-summary.md`

---

## Purpose

This summary provides a quick view of the Abbey Root lab.

It is intended for:

- project status checks
- documentation review
- AI context
- future BradCooke.com content
