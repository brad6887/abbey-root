# common.yml

Applies common baseline configuration to managed hosts.

This includes the global Git identity, fast-forward-only pull policy, automatic
remote pruning, and SSH transport for GitHub. Existing Abbey Root and Bread
Pitt checkouts can be audited and normalized with `abbey git`.
